# 1. Neustart für Zsh
sudo reboot

# 2. Nach Neustart - Powerlevel10k konfigurieren
p10k configure

# 3. Steam Proton aktivieren
# Steam → Settings → Steam Play → Enable for all titles

# 4. Syncthing konfigurieren
# Browser: http://127.0.0.1:8384

# 5. Tailscale aktivieren (falls gewünscht)
sudo tailscale up
